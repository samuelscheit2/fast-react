# Worker 678: DOM Hydration Replay Click Dispatch

Objective: connect private hydration replay target-claiming evidence to one blocked click replay dispatch diagnostic, preserving queue order and public hydration/event compatibility blockers.

First action: call `create_goal` with this worker objective before reading files, researching, or editing. Then call `get_goal` and include the active goal status/objective in your final report.

Write scope: `packages/react-dom/src/client/hydration-boundary-gate.js`, `packages/react-dom/src/events/`, hydration/event conformance tests, and `worker-progress/worker-678-dom-hydration-replay-click-dispatch.md`.

Keep scope to replayed click dispatch metadata; do not claim live event listener installation or public replay behavior.

Verification: focused hydration/event package tests, hydration conformance tests, event delegation oracle tests, `npm run check --workspace @fast-react/react-dom`, and `git diff --check`.
