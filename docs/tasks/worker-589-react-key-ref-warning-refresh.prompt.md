# Worker 589: React Key/Ref Warning Refresh

## Objective

Refresh React element key/ref warning diagnostics after cloneElement child-array
freeze parity, keeping compatibility claims narrow and false.

## Context

Read `WORKER_BRIEF.md`, `MASTER_PLAN.md`, and `MASTER_PROGRESS.md` first.
Worker 564 closed one cloneElement child-array freeze mismatch. This task should
audit and tighten nearby key/ref warning getter evidence.

## Write Scope

- `packages/react/element-factory.js`
- `tests/conformance/src/element-object-*.mjs`
- `tests/conformance/test/element-object-oracle.test.mjs`
- `tests/conformance/oracles/react-19.2.6-element-object-oracle.json` only if
  regenerated by the existing generator
- `worker-progress/worker-589-react-key-ref-warning-refresh.md`

Do not edit React DOM, scheduler, test-renderer, native, or Rust files.

## Requirements

- Verify and record development key/ref warning getter behavior for
  createElement, cloneElement, jsx, jsxs, and jsxDEV.
- Preserve cloneElement child-array freeze parity from worker 564.
- Keep `fastReactBehaviorCompatible`, full oracle claims, and compatibility
  flags false unless the local oracle framework already proves full coverage.
- Reject stale oracle artifacts by regenerating when needed.
- Commit all accepted changes on the worker branch before completing the goal.

## Verification

- `npm run element-object:generate --workspace @fast-react/conformance` if the
  oracle artifact changes
- `node --test tests/conformance/test/element-object-oracle.test.mjs`
- `npm run check --workspace @fast-react/react`
- `git diff --check`
