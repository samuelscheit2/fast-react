{
  "name": "@fast-react/conformance",
  "version": "0.0.0",
  "private": true,
  "description": "Initial deterministic inventory placeholder for the future Fast React dual-run conformance harness.",
  "type": "module",
  "scripts": {
    "inventory:placeholder": "node scripts/print-inventory.mjs --format=json",
    "inventory:placeholder:markdown": "node scripts/print-inventory.mjs --format=markdown",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
